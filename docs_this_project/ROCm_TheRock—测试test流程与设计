ROCm/TheRock — 测试（Test）流程与设计
CI 测试阶段架构总览 · 面向管理层的简明说明 · 生成于 2026-06-24
许可证：MIT（全部开源） 编排：GitHub Actions 驱动语言：Python 核心工具：CTest / pytest / GoogleTest
一、设计理念
测试体系采用 “配置驱动 + 分层编排 + 统一驱动器” 的设计：

① 配置驱动
一个 Python 脚本动态生成“测试矩阵”，决定测哪些组件、用哪台 GPU 机器、分几个分片（shard）。无需手改流水线。
② 分层编排
GitHub Actions 可复用工作流逐层调用：CI → 测试集 → 单组件。先跑 Sanity 闸门，通过后再并行铺开全部组件。
③ 统一驱动器
多数组件复用同一个 test_runner.py，靠 CTest 标签按 GPU 架构筛选用例；特殊组件才用专用脚本。
④ 产物复用
测试机不重新编译，而是直接下载构建阶段产出的 artifact 安装，缩短反馈周期。
二、整体调用链路

multi_arch_ci.yml   # CI 入口（push / PR / 手动触发）
└─ multi_arch_ci_linux.yml
   └─ test_artifacts.yml
      ├─ [Job] 生成测试矩阵 (ubuntu, 无 GPU)
      │   ├─ determine_rocm_test_dependencies.py  # 按改动筛选项目
      │   └─ fetch_test_configurations.py  # 生成各组件 JSON(含 test_script)
      ├─ [Job] Sanity 闸门 ─uses─▶ test_component.yml  # 必须先通过
      └─ [Job] 组件测试(矩阵×分片) ─uses─▶ test_component.yml  # 并行

  test_component.yml  # 在 GPU runner 的容器内执行
   ├─ 1. cleanup_processes.sh  # 前置清理
   ├─ 2. setup_venv.py + install_rocm_from_artifacts.py
   │      └─ fetch_artifacts.py  # 下载并安装 ROCm 产物
   ├─ 3. health_status.py / print_driver_gpu_info.py  # GPU 体检
   ├─ 4. ★ 执行 component.test_script ★
   └─ 5. reproduce_test_failure.py  # 失败时输出复现命令

  test_script 指向以下脚本之一  # build_tools/github_actions/test_executable_scripts/
   ├─ test_runner.py  ── 通用 ──▶ ctest --print-labels → 按 arch 过滤 → ctest
   └─ test_<component>.py  ── 专用 ──▶ pytest / GoogleTest / 专有可执行
颜色：蓝=可复用工作流 · 橙=Job · 紫=Python 脚本 · 绿=测试脚本/Shell
三、单组件测试执行步骤（test_component.yml）
1前置清理 — cleanup_processes.sh 清理残留 GPU 进程。
2环境搭建 — setup_venv.py 建虚拟环境；install_rocm_from_artifacts.py →（内部）fetch_artifacts.py 下载并安装构建产物。不重新编译。
3GPU 体检 — health_status.py、print_driver_gpu_info.py 确认驱动与 GPU 正常。
4执行测试 — 运行该组件的 test_script（核心步骤，支持分片 SHARD_INDEX/TOTAL_SHARDS 与重试）。
5失败复现 — 失败时 reproduce_test_failure.py 打印可在本地重跑的命令。
6后置清理 — 再次执行清理脚本释放 GPU。
四、测试框架：标准框架 vs 自研编排（核心结论）
结论：测试框架不是自研的。 真正执行用例的是业界标准开源框架（CTest、GoogleTest、pytest）； TheRock 自研的只是上层的 CI 编排与组件适配脚本——负责选机器、下载产物、分片、按 GPU 架构筛选用例、超时重试与失败复现，本身不实现断言逻辑。 一句话：“标准框架做测试，自研脚本做调度”。
① 编排 / 调度层（自定义）
GitHub Actions 可复用工作流 + Python 包装脚本（test_runner.py、test_<component>.py）。 只是“胶水”，用 subprocess 调下面的标准工具。test_runner.py 通过 ctest --print-labels 按 GPU 架构动态选用例，是 TheRock 自己的逻辑。
② 测试引擎层（标准、非自研）
CTest / CMake 测试调度器，按 label 过滤
GoogleTest C++ 断言引擎（多数库的底层可执行）
pytest Python 测试框架
组件自带测试器 如 ROCgdb 的 GDB 用例
层	是否自研	组成	职责
编排 / 调度	自研	Actions 工作流 + test_runner.py / test_*.py	选机器、装产物、分片、按 arch 筛选、重试、失败复现
测试引擎	标准开源	CTest / CMake、GoogleTest、pytest	真正运行用例、执行断言、判定通过/失败
五、概念厘清：框架 / 调度器 / 脚本 / 胶水（各司其职）
常见误解：把 test_runner.py 当“测试框架”、把 CTest 当“测试脚本”。实际定位不同—— GoogleTest/pytest 才是框架，CTest 是调度器，CTestTestfile.cmake 是脚本，test_runner.py 是 CI 胶水。
角色	对应物	是否自研	职责
测试框架
Framework	GoogleTest pytest	标准开源	提供断言、用例发现与组织；case 本体写在这里
测试调度器
Driver	CTest / CMake	标准开源	发现、按 label 过滤、调度并运行用例；不写断言
测试脚本
Test script	CTestTestfile.cmake	CMake 生成	用 add_test() 注册每个 case 并打 label，随产物分发
CI 胶水
Glue	test_runner.py / test_*.py	自研	组装并调用 ctest 命令；选机器/分片/筛选/重试，不含测试逻辑
Case 如何定义（构建期，3 层）
① 源码层
TEST()/TEST_P()(GoogleTest) 或 def test_*(pytest) 写断言 → case 本体。
② 注册层
CMakeLists.txt 用 add_test()/gtest_discover_tests() 注册进 CTest，并 set_tests_properties(... LABELS ...) 打标签。分类标签 quick/standard/… 来自 test_categories.yml。
③ 安装层
构建产物生成 bin/<component>/CTestTestfile.cmake，随 artifact 分发到测试机。
Case 如何选择（运行期，test_runner.py 逻辑 = CTest label 过滤）

# 步骤 1：发现 label
ctest --print-labels / ctest -N   →  GPU架构 gpu_gfxXXXX、exclude 标签

# 步骤 2：确定档位
category = TEST_TYPE  (quick / standard / comprehensive / full)

# 步骤 3：组装并执行 ctest 命令
ctest -L <category>            →  选该档位用例
      -L ex_gpu_<当前架构>     →  叠加本机 GPU 套件
      -LE <category>_exclude    →  排除不适用用例
      --timeout / -j            →  超时 / 并行

# 步骤 4：分片
GTEST_SHARD_INDEX / GTEST_TOTAL_SHARDS   →  交给 GoogleTest 自己切片
  
本质：用 -L(包含) 与 -LE(排除) 按「测试档位 × GPU 架构」筛出当前子集，再按 shard 切片。
六、代码归属：谁放“怎么测”，谁放“测什么”
TheRock 主仓只放“怎么测”的调度逻辑；“测什么”（case 本体 + 分类标签）分散在各 submodule 里。 构建后随 artifact 以 CTestTestfile.cmake 落到测试机，再由 test_runner.py 调 CTest 按标签筛选执行。
归属对照
内容	所在位置
测试 case 源码（TEST() / def test_*）	各组件 → 主要在 rocm-libraries / rocm-systems 两个 submodule
CTest 注册 + 分类标签（add_test、test_categories.yml）	与 case 源码同仓（同上 submodule）
CI 编排胶水（test_runner.py、工作流）	TheRock 主仓（build_tools/、.github/）
Submodule 分布（共 10 个）
重要：所有 submodule 的测试 case 都不在 TheRock 主仓，而在各自上游仓库。 TheRock 仅通过 git submodule 引用它们（记录 URL + commit SHA 指针）， 需 git submodule update --init 或 fetch_sources.py 才会拉到本地。 下表“聚合度”列说明区别仅在于多库合一还是一仓一件。
Submodule	内容	聚合度	含测试 case
rocm-libraries (develop)	rocBLAS、hipBLASLt、MIOpen、rocRAND、rocFFT、rocSPARSE… 数学/ML 库	monorepo（多库合一）	✓ 在独立仓
rocm-systems (develop)	HIP、CLR、ROCr、rocprofiler、RCCL… 系统/运行时	monorepo（多库合一）	✓ 在独立仓
compiler/amd-llvm、half、hipify、rocm-cmake、amd-mesa 等	编译器与基础组件	单组件仓（一仓一件）	✓ 在独立仓
统一结论：case 一律在各自上游 submodule 仓、不在 TheRock 主仓；区别只是 rocm-libraries/rocm-systems 是“多库合一”的 monorepo，其余是“一仓一件”。

TheRock 主仓 (build_tools/ , .github/)  # 怎么测：调度逻辑
└─ git submodule
   ├─▶ rocm-libraries/  rocBLAS/MIOpen/… 源码 + test case + test_categories.yml
   └─▶ rocm-systems/  HIP/RCCL/… 源码 + test case + test_categories.yml
        │
        ▼ 构建产出
   artifact: bin/<component>/CTestTestfile.cmake  # 测什么：注册+标签
        │
        ▼ 测试机下载安装
   test_runner.py ─▶ ctest -L/-LE  # 按标签筛选执行
  
七、测试脚本框架与调用工具一览
测试脚本	脚本框架	底层调用的工具	覆盖组件 / 说明
test_runner.py	Python · subprocess	CTest → GoogleTest	通用驱动器。ctest --print-labels 发现 GPU 架构标签 → 按 TEST_TYPE(quick/standard/…) 过滤 → 跑 ctest。覆盖 rocprim、rocblas、rocthrust、miopen、hipcub、hipsparse(部分) 等多数组件。
test_sanity.py	pytest	subprocess	Sanity 闸门，快速验证安装可用，先于其它测试运行。
test_hiptests.py	Python · subprocess	CTest	HIP 核心运行时测试。
test_rccl.py	pytest	GoogleTest	RCCL 集合通信库，pytest 包裹 gtest 可执行。
test_tensilelite.py	pytest	subprocess	TensileLite（rocBLAS 内核生成）测试。
test_hipblaslt.py	Python · subprocess	GoogleTest	hipBLASLt，直接驱动 gtest 可执行。
test_rocfft.py / test_amdsmi.py	Python · subprocess	GoogleTest CTest	rocFFT / AMD-SMI。
test_hipdnn_integration_tests.py	Python · subprocess	CTest	hipDNN 集成测试（另有 install / samples 子脚本）。
test_rocgdb.py	Python · subprocess	hipcc 编译 + GDB 用例	调试器 ROCgdb，先用 hipcc 编译再跑 gdb 测试集。
test_rocr-debug-agent.py	Python · subprocess	专有可执行	ROCr 调试 agent。
test_rocsparse.py / test_hipsparse.py /
test_rocprofiler_sdk.py 等	Python · subprocess	组件自带测试器	各组件专用测试逻辑（含已知问题的临时绕过）。
说明：所有专用脚本本质都是 Python 用 subprocess 包装底层测试工具；“脚本框架”指脚本自身组织方式，“底层工具”指真正执行用例的引擎。
八、测试规模控制机制
测试类型（TEST_TYPE）
quick / standard / comprehensive / full，逐级扩大用例范围。PR 用轻量级，夜间/发布用全量。
分片（Sharding）
单组件按 TOTAL_SHARDS 拆分并行执行，缩短墙钟时间。
按改动筛选
determine_rocm_test_dependencies.py 依据改动项目只跑相关测试。
多架构矩阵
按 AMD GPU 家族（gfx94X / gfx120X 等）分别分配测试机与用例标签。
九、开源与可复现性
✓ 全部测试脚本均开源（MIT 许可），位于仓库 build_tools/github_actions/test_executable_scripts/ 与 test_tools/，可在本地复现。
唯一外部依赖：测试矩阵的额外配置仓库 ROCm/therock-ci-config（设为 continue-on-error，缺失也能运行）；自托管 GPU 运行机与少量 secrets（基准测试数据库、AWS 凭证）属内部基础设施，不公开。
来源：ROCm/TheRock 仓库 .github/workflows/ 与 build_tools/github_actions/。本文档为只读分析，未修改任何代码。