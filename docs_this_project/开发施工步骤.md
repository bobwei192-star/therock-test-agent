我建议第一版就按你现在方向走：OpenCode 原生 agents + shell 执行 + JSON 状态文件 + 报告模板。不要一开始做复杂 Python 包或深 fork。等粗糙版跑通后，再把稳定逻辑沉淀成 Python CLI。 快速做个粗糙版  并记得打log 这样方便我迭代  现在基于/home/zx/TheRock_CI测试流程/therock-test-agent 项目的情况 开始开发吧 请先写个开发施工步骤

我会按“先能跑、能恢复、能打日志、能出报告”的顺序拆施工步骤；同时基于当前项目已有 install.sh、.opencode overlay 和 component_sort_order.json，避免设计成复杂 Python 包。

当前项目实际只有一个 .opencode/agents/therock-loop.md，没有外置执行器源码，也没有 commands/tools 目录；所以第一步应该先补最小 shell runner，再让 OpenCode agent 调它。

可以，先按“粗糙版能跑”为目标施工。你现在的方向是对的：OpenCode 原生 agent + shell 执行 + JSON 状态文件 + 报告模板，不是深 fork。

不过当前项目有一个现实问题：install.sh 里写了会注入多个 agent/tools，但现在实际只有 .opencode/agents/therock-loop.md，没有 therock-orchestrator.md、therock-test-executor.md、.opencode/tools/*、agent-src/therock-test-agent。所以第一步不是写复杂架构，而是把最小可运行闭环补齐。

开发施工步骤
1. 先补齐最小目录结构
目标是让 install.sh 说的东西真实存在。

建议新增：

therock-test-agent/
  .opencode/
    agents/
      therock-loop.md
    commands/
      therock-run.md
      therock-resume.md
      therock-report.md
    tools/
      therock_agent.sh
      detect-gpu-timeout.sh
      generate-report.sh
  docs_this_project/
    component_sort_order.json
    需求.md
第一版不需要 therock_loop_engine/cli/state/scheduler/... 这种复杂 Python 包。

2. 修正 install.sh
当前 install.sh 有两处需要调整：

不要打印不存在的 agent 文件。
如果没有 agent-src/therock-test-agent，就不要把它当核心路径。
第一版安装逻辑应该是：

clone / update OpenCode。
拷贝 .opencode/agents、.opencode/commands、.opencode/tools 到 OpenCode 项目。
安装依赖。
提示用户进入 OpenCode 后执行 @therock-loop 或命令。
OpenCode 官方支持项目级 agents、commands、permissions，这个方向和官方能力匹配。参考：OpenCode docs。

3. 写核心 shell runner
新增一个粗糙但稳定的脚本：

.opencode/tools/therock_agent.sh
支持这些命令：

therock_agent.sh init
therock_agent.sh run
therock_agent.sh resume <run_id>
therock_agent.sh report <run_id>
第一版职责：

解析参数。
识别 artifacts path：
/output-linux-portable/build
/output/build
统一得到：
build_root=<用户传入路径>
rocm_dist=<build_root>/dist/rocm
读取 component_sort_order.json。
默认跳过 "gpu_hang_risk": true。
生成 runs/<run_id>/global_state.json。
串行执行 task。
每个 task 前后写状态。
stdout/stderr 落盘。
生成 summary 和 failure report。
4. 先做最小 task 执行
第一版不要追求覆盖所有 TheRock 脚本细节，先做最小规则：

task = test_type + component
执行时优先调用 TheRock 官方测试脚本或 test_runner.py。如果找不到对应入口，就标记为 blocked，不要让 agent 瞎猜。

每个任务必须输出：

logs/<task_id>.stdout.log
logs/<task_id>.stderr.log
并更新：

global_state.json
5. 实现 loop 收敛
严格按你的规则：

第一轮跑全部允许任务。
下一轮只跑上一轮失败任务。
如果失败集为空，停止。
如果连续 3 轮失败集完全一致，停止。
通过的任务不再重跑。
这个 loop 不能只写在 prompt 里，必须由 therock_agent.sh 或一个最小脚本控制。

6. 实现 GPU 高风险默认跳过
直接用：

"gpu_hang_risk": true
默认行为：

skip
用户首次传参显式允许时才执行：

--gpu-risk include
或：

--gpu-risk quarantine
quarantine 的含义是：普通任务跑完后，再单独跑高风险任务。

7. 实现修改记录
你允许修改：

TheRock/build_tools/**
第一版要求：

每次修改前复制备份。
保存 diff。
写入 file_changes.jsonl。
报告里列出改了什么、为什么改、哪一轮改的。
禁止默认修改：

TheRock 源码组件目录
ROCm 子模块源码
dist/rocm/lib
dist/rocm/bin
8. 失败报告套模板
每个最终失败 task 生成：

failures/<task_id>_failure_report.md
内容直接套 1问题描述格式.md，不要另起格式。

第一版可以先填这些字段：

问题标题
组件与测试信息
原始失败结果
测试环境
复现 / 复测步骤
复测结果
问题关键 log
问题原因解释
CI 处理建议
附件与证据
9. 改造 .opencode/agents/therock-loop.md
现在这个 agent prompt 让 LLM 自己执行 loop，风险偏高。

建议改成：

agent 不直接维护 loop 状态。
agent 只调用：
.opencode/tools/therock_agent.sh run ...
agent 负责解释结果、总结日志、生成建议。
bash 权限只允许调用 .opencode/tools/therock_agent.sh 和少数只读命令。
这样更符合 OpenCode permissions 机制。

10. 增加 OpenCode commands
新增：

.opencode/commands/therock-run.md
.opencode/commands/therock-resume.md
.opencode/commands/therock-report.md
作用：

/therock-run：启动测试。
/therock-resume：恢复某个 run。
/therock-report：重新生成报告。
这样比每次手写 prompt 更稳定。

第一轮开发顺序
我建议按这个顺序施工：

修 install.sh，让 overlay 目录和实际文件一致。
新增 .opencode/tools/therock_agent.sh。
实现 init + run_id + runs/ 输出目录。
实现 artifacts path 解析。
实现读取 component_sort_order.json 并跳过 gpu_hang_risk=true。
实现单 task 执行和日志落盘。
实现 global_state.json 每 task 前后更新。
实现失败集 loop。
实现 resume <run_id>。
实现 summary report。
实现失败报告套模板。
最后再优化 OpenCode agent prompt 和 commands。
我的判断
你的方案能实现 loop，但关键点是：

OpenCode 负责“调度意图和分析”，shell runner 负责“确定性执行和状态”。

不要让 LLM 纯靠上下文记住上一轮失败集。只要 global_state.json 和日志落盘做好，粗糙版就能快速迭代，也方便你后面汇报效率提升。