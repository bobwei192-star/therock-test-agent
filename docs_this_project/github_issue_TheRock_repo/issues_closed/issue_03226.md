# #3226 - Expose FLANG_PARALLEL_COMPILE_JOBS in top-level CMakeLists.txt

**State**: closed  
**Author**: stellaraccident  
**Created**: 2026-02-03T17:50:11Z  
**Updated**: 2026-02-04T09:48:57Z  
**Labels**: enhancement  
**Assignees**: lumachad  
**URL**: https://github.com/ROCm/TheRock/issues/3226  

---

## Problem

The `FLANG_PARALLEL_COMPILE_JOBS` flag exists in `compiler/CMakeLists.txt` to limit Flang build concurrency (which is very memory-intensive), but it's buried and easy to forget about.

When not set, it prints a warning:
```
Flang concurrency is unlimited. Memory usage may be much higher.
```

## Suggestion

Add this option to the top-level `CMakeLists.txt` alongside other build configuration options so it's discoverable and documented in one place.

Something like:
```cmake
set(FLANG_PARALLEL_COMPILE_JOBS "" CACHE STRING
  "Limit parallel compile jobs for Flang (reduces memory usage on systems with many cores)")
```

This would make it visible in `ccmake`/`cmake-gui` and easier to find when configuring builds.