# #958 - [CICD]: Enable `SPARSE` tests

**State**: closed  
**Author**: geomin12  
**Created**: 2025-07-01T23:14:25Z  
**Updated**: 2025-07-09T16:07:02Z  
**Assignees**: geomin12  
**URL**: https://github.com/ROCm/TheRock/issues/958  

---

Currently, `hipSPARSE` and `rocSPARSE` are enabled and executables are appearing in builds. However, tests are failing due to missing matrices

For `rocSPARSE`, I am able to run some tests in [this `users/geomin12/adding-sparse`](https://github.com/ROCm/TheRock/compare/users/geomin12/adding-sparse?expand=1) branch, however, it stops and requests for matrices:

```
Opening file '/home/geomin12/Code/TheRock/therock-build/bin/../matrices/nos1.csr' ...
#
# error:
# cannot open file '/home/geomin12/Code/TheRock/therock-build/bin/../matrices/nos1.csr'
#
# PLEASE READ CAREFULLY !
#
# What could be the reason of this error:
# -1 If you are running the benchmarking application, then the path (or the name) of the file you have specified might contain a typo.
# -2 If you are running the benchmarking application, then the file you have specified does not exist.
# -3 If you are running the testing application, then it expects to find the file at the specified location. This means that either you did not download the test matrices, or you did not specify the location of the folder containing your files. If you want to specify the location of the folder containing your files, then you will find the needed information with 'rocsparse-test --help'.If you need to download matrices, then a cmake script 'rocsparse_clientmatrices.cmake' is available from the rocsparse client package.
#
# Examples: 'rocsparse_clientmatrices.cmake -DCMAKE_MATRICES_DIR=<path-of-your-folder>'
#           'rocsparse-test --matrices-dir <path-of-your-folder>'
# (or        'export ROCSPARSE_CLIENTS_MATRICES_DIR=<path-of-your-folder>;rocsparse-test')
```

For `hipSPARSE`, the executable immediately errors and searches for matrices. 

The matrices are being fetched, but not packaged into S3 yet and can't be found during tests. We may need to patch the packages CMake configs and determine what license the matrices are to be distributed under.

Once we have these matrices, the tests will be able to run properly