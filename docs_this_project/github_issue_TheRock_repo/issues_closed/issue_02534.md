# #2534 - [Issue] Windows pytorch tests failing with `RuntimeError: No HIP GPUs are available` on `windows-gfx110X-gpu-rocm` runners

**State**: closed  
**Author**: srayasam-amd  
**Created**: 2025-12-12T11:29:52Z  
**Updated**: 2025-12-12T22:35:06Z  
**Labels**: CICD, platform: Windows, gfx110X-dgpu, ecosystem: PyTorch  
**Assignees**: amd-justchen  
**URL**: https://github.com/ROCm/TheRock/issues/2534  

---

**Run:** https://github.com/ROCm/TheRock/actions/runs/20155986838

**Jobs:**

- https://github.com/ROCm/TheRock/actions/runs/20158908554/job/57871427590
- https://github.com/ROCm/TheRock/actions/runs/20158908554/job/57871109979
- https://github.com/ROCm/TheRock/actions/runs/20158908554/job/57871980749

**Effected gfx:**

- 3.11
- 3.12
- 3.13

**Pytorch Versions**

- nightly

**Error Snippet:**
=================================== ERRORS ====================================
_________ ERROR at teardown of TestConvolutions.test_conv_transpose2d _________

self = <pytorch_smoke_test.TestConvolutions object at 0x000001D47C0DB4D0>

    def teardown_method(self):
        # TODO(#999): fix tests stalling on exit without this
>       torch.cuda.synchronize()

external-builds\pytorch\smoke-tests\pytorch_smoke_test.py:104: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv\Lib\site-packages\torch\cuda\__init__.py:1081: in synchronize
    _lazy_init()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

    def _lazy_init():
        global _initialized, _queued_calls
        if is_initialized() or hasattr(_tls, "is_initializing"):
            return
        with _initialization_lock:
            # We be double-checked locking, boys!  This is OK because
            # the above test was GIL protected anyway.  The inner test
            # is for when a thread blocked on some other thread which was
            # doing the initialization; when they get the lock, they will
            # find there is nothing left to do.
            if is_initialized():
                return
            # It is important to prevent other threads from entering _lazy_init
            # immediately, while we are still guaranteed to have the GIL, because some
            # of the C calls we make below will release the GIL
            if _is_in_bad_fork():
                raise RuntimeError(
                    "Cannot re-initialize CUDA in forked subprocess. To use CUDA with "
                    "multiprocessing, you must use the 'spawn' start method"
                )
            if not hasattr(torch._C, "_cuda_getDeviceCount"):
                raise AssertionError("Torch not compiled with CUDA enabled")
            if _cudart is None:
                raise AssertionError(
                    "libcudart functions unavailable. It looks like you have a broken build?"
                )
            # This function throws if there's a driver initialization error, no GPUs
            # are found or any other error occurs
>           torch._C._cuda_init()
E           RuntimeError: No HIP GPUs are available
