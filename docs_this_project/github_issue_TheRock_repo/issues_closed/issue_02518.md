# #2518 - [Issue]: libatomic installation needed on certain distros for rocminfo to work following tarball install for ROCm 7.10

**State**: closed  
**Author**: lsudarsh-amd  
**Created**: 2025-12-11T03:49:31Z  
**Updated**: 2026-06-09T14:03:07Z  
**Labels**: status: assessed  
**Assignees**: darren-amd  
**URL**: https://github.com/ROCm/TheRock/issues/2518  

---

### Problem Description

On clean installations of distros like RHEL 8.10, 10.0 and SLES 15.7, after installing ROCm 7.10 using tarball method, commands like rocminfo do not work due to a missing libatomic dependency. If libatomic is installed after the fact, then rocminfo works. This only appears to affect these 3 distros, the other supported distros do not need libatomic to be explicitly installed for things to work as expected.

Here is a log of the failure:
rocminfo
rocminfo: error while loading shared libraries: libatomic.so.1: cannot open shared object file: No such file or directory

### Operating System

RHEL 8.10, 10.0 and SLES 15.7

### CPU

Intel Core i7-10700

### GPU

AMD Radeon RX 7900 XT

### ROCm Version

ROCm 7.10 Prerelease

### ROCm Component

_No response_

### Steps to Reproduce

1. Clean OS install of RHEL 8.10, 10.0 or SLES 15.7 from distro ISO

2. Follow the steps to install tarball
sudo usermod -a -G render,video $LOGNAME
mkdir therock-tarball && cd therock-tarball

wget https://repo.amd.com/tarball/therock-dist-linux-gfx950-dcgpu-7.10.0rc2.tar.gz

mkdir install
tar -xf *.tar.gz -C install

cd therock-tarball
ROCM_INSTALL_PATH=$(pwd)/install
sudo tee /etc/profile.d/set-rocm-env.sh << EOF
export ROCM_PATH=$ROCM_INSTALL_PATH
export PATH=$PATH:$ROCM_PATH/bin
export LD_LIBRARY_PATH=$ROCM_PATH/lib
EOF
sudo chmod +x /etc/profile.d/set-rocm-env.sh

3. Reboot system for all changes to take effect

4. Run rocminfo

### (Optional for Linux users) Output of /opt/rocm/bin/rocminfo --support

_No response_

### Additional Information

_No response_