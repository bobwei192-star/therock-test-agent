# #1900 - [Issue]: rocSPARSE fails to find rocBLAS due to version mismatch

**State**: closed  
**Author**: umarinkovic  
**Created**: 2025-10-23T15:21:43Z  
**Updated**: 2025-11-13T09:16:44Z  
**URL**: https://github.com/ROCm/TheRock/issues/1900  

---

From **rocSPARSE_configure.log**:

`[rocSPARSE configure] -- Build rocSPARSE with rocBLAS is disabled since rocBLAS is not found`

Turning off quiet mode from `find_package` in **rocSPARSE** reveals:

```bash
[rocSPARSE configure] -- Resolving super-project find_package(rocblas 4.1.0 BYPASS_PROVIDER NO_DEFAULT_PATH PATHS C:/dev/repos/TheRock/out/build/math-libs/BLAS/rocBLAS/dist/lib/cmake/rocblas)
[rocSPARSE configure] CMake Warning at C:/dev/repos/TheRock/cmake/therock_subproject_dep_provider.cmake:60 (find_package):
[rocSPARSE configure]   Could not find a configuration file for package "rocblas" that is
[rocSPARSE configure]   compatible with requested version "4.1.0".
[rocSPARSE configure] 
[rocSPARSE configure]   The following configuration files were considered but not accepted:
[rocSPARSE configure] 
[rocSPARSE configure]     C:/dev/repos/TheRock/out/build/math-libs/BLAS/rocBLAS/dist/lib/cmake/rocblas/rocblas-config.cmake, version: 5.1.0
[rocSPARSE configure] 
[rocSPARSE configure] Call Stack (most recent call first):
[rocSPARSE configure]   CMakeLists.txt:206 (find_package)
[rocSPARSE configure] 
[rocSPARSE configure] 
[rocSPARSE configure] -- Super-project find_package(rocblas VERSION 4.1.0) -> C:/dev/repos/TheRock/out/build/math-libs/BLAS/rocBLAS/dist/lib/cmake/rocblas/rocblas-config-version.cmake
[rocSPARSE configure] CMake Warning at CMakeLists.txt:206 (find_package):
[rocSPARSE configure]   Could not find a configuration file for package "rocblas" that is
[rocSPARSE configure]   compatible with requested version "4.1.0".
[rocSPARSE configure] 
[rocSPARSE configure]   The following configuration files were considered but not accepted:
[rocSPARSE configure] 
[rocSPARSE configure]     C:/dev/repos/TheRock/out/build/math-libs/BLAS/rocSPARSE/prefix/rocblasConfig.cmake, version: 5.1.0
[rocSPARSE configure] 
[rocSPARSE configure] 
[rocSPARSE configure] 
```

rocBLAS is an optional dependency so build proceeds with a warning;

From **rocm-libraries/projects/rocsparse/CMakeListst.txt**

```cmake
# Find rocblas package
if (BUILD_WITH_ROCBLAS)
  find_package(rocblas 4.1.0 QUIET)
else()
  message("-- Build rocSPARSE with rocBLAS is disabled")
endif()
# ...
## add dependency on rocBLAS
if (rocblas_FOUND)
  set(ROCBLAS_MINIMUM "4.1.0")
  rocm_package_add_dependencies(SHARED_DEPENDS "rocblas >= ${ROCBLAS_MINIMUM}")
  rocm_package_add_deb_dependencies(STATIC_DEPENDS "rocblas-static-dev >= ${ROCBLAS_MINIMUM}")
  rocm_package_add_rpm_dependencies(STATIC_DEPENDS "rocblas-static-devel >= ${ROCBLAS_MINIMUM}")
else()
  message("-- Build rocSPARSE with rocBLAS is disabled since rocBLAS is not found")
endif()
```