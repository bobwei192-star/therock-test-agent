# #665 - [Feature]: Need to be able to build and test a subset of theRock cmake

**State**: closed  
**Author**: amd-chrissosa  
**Created**: 2025-05-20T06:32:51Z  
**Updated**: 2025-06-30T23:25:40Z  
**Assignees**: geomin12  
**URL**: https://github.com/ROCm/TheRock/issues/665  

---

### Suggestion Description

We'd like to enable presubmit with the rock for https://github.com/ROCm/rocm-libraries starting with rocPrim. In order to enable fast presubmits, we don't need or want to build all of ROCm if a CL is uploaded for rocPrim. Instead we need to change the workflows to enable a flag to specify which projects need to be built and we need a mapping of projects to cmake targets. At least for now we could test: -DTHEROCK_ENABLE_PRIM=ON with all other flags false but ideally, we'd get at a more granular level.

Anyway,

Step 1 - get it work for rocPrim - build first
Step 2  - get it work generally for any project in rocm-libraries for build
Step 3 - get it to work with tests.

### Operating System

Windows/Linux

### GPU

gfx942

### ROCm Component

rocPrim