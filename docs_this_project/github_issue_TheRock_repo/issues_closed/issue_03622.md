# #3622 - [Multi-arch] Windows `The self-hosted runner lost communication with the server.` build errors

**State**: closed  
**Author**: ScottTodd  
**Created**: 2026-02-25T22:59:26Z  
**Updated**: 2026-03-27T21:05:28Z  
**Labels**: bug, platform: Windows, build issue  
**Assignees**: ScottTodd, amd-justchen, amd-shiraz  
**URL**: https://github.com/ROCm/TheRock/issues/3622  

---

Seeing a few instances of this: 

* https://github.com/ROCm/TheRock/actions/runs/22406835557

    ```
    Windows::release / Build Multi-Arch Stages / math-libs (gfx1151, gfx1151, windows-gfx1151-gpu-rocm, false) / Stage - Math Libs (gfx1151)
    The self-hosted runner lost communication with the server. Verify the machine is running and has a healthy network connection. Anything in your workflow that terminates the runner process, starves it for CPU/Memory, or blocks its network access can cause this error.
    ```
     
* https://github.com/ROCm/TheRock/actions/runs/22369923525
    
    ```
    Windows::release / Build Multi-Arch Stages / math-libs (gfx120X-all, false) / Stage - Math Libs (gfx120X-all)
    The self-hosted runner lost communication with the server. Verify the machine is running and has a healthy network connection. Anything in your workflow that terminates the runner process, starves it for CPU/Memory, or blocks its network access can cause this error.
    ```

Seems unique to Windows. I made the CMake build options the same between "CI" and "Multi-arch CI", but multi-arch CI does NOT use ccache currently. That might be putting more pressure on the system CPU/memory.