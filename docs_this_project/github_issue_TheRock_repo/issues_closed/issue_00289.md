# #289 - [Issue]: hipblasLT linking issue

**State**: closed  
**Author**: geomin12  
**Created**: 2025-03-25T17:24:02Z  
**Updated**: 2025-03-25T20:35:29Z  
**Labels**: bug  
**Assignees**: marbre  
**URL**: https://github.com/ROCm/TheRock/issues/289  

---

### Problem Description

On 3/24/2025 5pm PST, the build artifact `13979545550` is able to run and pass hipblaslt tests.

As of 3/25/2025 9am PST, the build artifact `14048712380`, the tests `hipblaslt-tests` are unable to link to `hipblaslt` library, error:

```
./build/artifacts/output_dir/bin/hipblaslt-test: error while loading shared libraries: libhipblaslt.so.0: cannot open shared object file: No such file or directory
```

It is possible that the original `13979545550` is linking `hipblaslt` library to `/opt/rocm`. Although, I did run both artifact IDs in a docker image, and here's the output:

`14048712380`: 
![Image](https://github.com/user-attachments/assets/e606ce97-8630-4a74-a6c5-8f0715b16252)

`13979545550`:
![Image](https://github.com/user-attachments/assets/f59cf30d-0ed3-418e-be1d-9b1640877231)

so seems like it was previously linking correctly, but not anymore!

### Operating System

Ubuntu

### CPU

(not sure), mi300 on GH cluster

### GPU

Mi300

### ROCm Version

ADHOCBUILD

### ROCm Component

_No response_

### Steps to Reproduce

I have been using this script and switching out artifact IDs to test both packages:

```
#!/bin/bash
VARIANT="_generic"
TARGET_VARIANT="_gfx94X-dcgpu"
BUILD_ARTIFACTS_DIR="build/artifacts"
# ARTIFACT_RUN_ID="14048712380"
ARTIFACT_RUN_ID="13979545550"

echo "Making ${BUILD_ARTIFACTS_DIR} directory"
mkdir -p "${BUILD_ARTIFACTS_DIR}"

echo "Downloading artifacts"

aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/core-runtime_run"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/core-runtime_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/sysdeps_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/base_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/amd-llvm_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/core-hip_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/host-blas_lib"${VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/blas_lib"${TARGET_VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request
aws s3 cp s3://therock-artifacts/${ARTIFACT_RUN_ID}/blas_test"${TARGET_VARIANT}".tar.xz "${BUILD_ARTIFACTS_DIR}" --no-sign-request

echo "Unpacking artifacts"
pushd "${BUILD_ARTIFACTS_DIR}"
mkdir output_dir
python /install-googletest/TheRock/build_tools/fileset_tool.py artifact-flatten *.tar.xz -o output_dir --verbose
popd
```

and running on this docker image (just so some libs don't link to `opt/rocm`): ` sudo docker run --device=/dev/kfd --device=/dev/dri --group-add video -i -t ghcr.io/rocm/therock_build_manylinux_x86_64:main /bin/bash`

You can also use this [test workflow](https://github.com/ROCm/TheRock/actions/workflows/test_linux_packages.yml) and pass in the artifact run ids

### (Optional for Linux users) Output of /opt/rocm/bin/rocminfo --support

_No response_

### Additional Information

_No response_