# #3131 - [ci] Re-enable `linux-strix-halo-gpu-rocm-1` and `linux-strix-halo-gpu-rocm-2` once docker issues are resolved

**State**: open  
**Author**: geomin12  
**Created**: 2026-01-28T10:32:01Z  
**Updated**: 2026-05-18T21:36:38Z  
**Labels**: disabled-test  
**Assignees**: geomin12  
**URL**: https://github.com/ROCm/TheRock/issues/3131  

---

As of now, `linux-strix-halo-gpu-rocm-1` (CHRN-SI-165) and `linux-strix-halo-gpu-rocm-2` (CHRN-SI-164) are running into issues such as:

```
Checking docker version
Clean up resources from previous jobs
Create local container network
  /usr/bin/docker network create --label 3b0ade github_network_ecca57a962a146f78a64747a70b3cdfb
  Error response from daemon: all predefined address pools have been fully subnetted
  Error: Docker network create failed with exit code 1
```

during the test docker initialization step. For some reason, I cannot connect to them from Korea but will retry once in the US.

Most likely, the machine created too many networks and never cleaned them up. Will have to go into the machine to clean it up and do a permanent step to largen the docker address pool (will do with all strix halo linux machines)

The two machines are disabled until resolved