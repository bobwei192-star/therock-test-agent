# #1899 - [Issue]: Lower than expected conv2d performance on Windows build

**State**: open  
**Author**: kasper93  
**Created**: 2025-10-23T14:50:17Z  
**Updated**: 2026-06-23T08:39:36Z  
**Labels**: status: triage, project: miopen  
**Assignees**: adityas-amd  
**URL**: https://github.com/ROCm/TheRock/issues/1899  

---

### Problem Description

GPU: AMD Radeon RX 9070 XT



### Operating System

Windows 10.0.26100

### CPU

AMD Ryzen 9 7950X 16-Core Processor

### GPU

AMD Radeon RX 9070 XT

### ROCm Version

Pytorch wheel: 2.10.0a0+rocm7.10.0a20251018

Are there still missing things and limitations of those Windows builds?

### Steps to Reproduce

1. Run

``` python
import torch
import time

# Convolution settings
batch_size = 32
height = 256
width = 256
in_channels = 64
out_channels = 64
kernel_size = (3, 3)
stride = (1, 1)
padding = 1

# Number of warmup and measured runs
warmup = 2
runs = 10

# Dtypes to test
dtypes = {
    "float32": torch.float32,
    "float16": torch.float16,
    "bfloat16": torch.bfloat16,
}


def benchmark(dtype_name, dtype):
    print(f"\nTesting {dtype_name}...")

    # Initialize input and kernel
    x = torch.randn((batch_size, height, width, in_channels), dtype=dtype, device="cuda")
    w = torch.randn((*kernel_size, in_channels, out_channels), dtype=dtype, device="cuda")

    x = x.permute(0, 3, 1, 2)  # NHWC -> NCHW
    w = w.permute(3, 2, 0, 1)  # HWIO -> OIHW

    conv = torch.nn.functional.conv2d

    # Warmup
    for _ in range(warmup):
        y = conv(x, w, stride=stride, padding=padding)
        torch.cuda.synchronize()

    # Timed run
    start = time.time()
    for _ in range(runs):
        y = conv(x, w, stride=stride, padding=padding)
        torch.cuda.synchronize()
    end = time.time()

    total_time = end - start
    avg_time = total_time / runs

    # Approx FLOPs for Conv2D: 2 * H * W * Cout * (Cin * Kx * Ky) * B
    flops = 2 * height * width * out_channels * (in_channels * kernel_size[0] * kernel_size[1]) * batch_size
    tflops = flops / avg_time / 1e12

    print(f"Avg time: {avg_time:.6f} s, Throughput: {tflops:.2f} TFLOP/s")


for name, dtype in dtypes.items():
    try:
        benchmark(name, dtype)
    except Exception as e:
        print(f"Skipped {name} due to error: {e}")
```

2. Observe results

Windows: 
```
Testing float32...
Avg time: 0.981738 s, Throughput: 0.16 TFLOP/s

Testing float16...
Avg time: 0.791462 s, Throughput: 0.20 TFLOP/s

Testing bfloat16...
Avg time: 0.758391 s, Throughput: 0.20 TFLOP/s
```

The same on Linux (WSL) those values are probably expected:
```
Testing float32...
Avg time: 0.091828 s, Throughput: 1.68 TFLOP/s

Testing float16...
Avg time: 0.020888 s, Throughput: 7.40 TFLOP/s

Testing bfloat16...
Avg time: 0.010952 s, Throughput: 14.12 TFLOP/s
```

Testcase were converted from sibling issue on JAX, https://github.com/ROCm/rocm-jax/issues/82