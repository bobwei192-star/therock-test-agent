# #2904 - [Issue]: Broken install using `uv` + pyproject.toml

**State**: open  
**Author**: kunaltyagi  
**Created**: 2026-01-14T08:58:01Z  
**Updated**: 2026-04-10T19:08:31Z  
**Labels**: status: assessed, ecosystem: PyTorch  
**URL**: https://github.com/ROCm/TheRock/issues/2904  

---

### Problem Description

Using `torch==2.11.0a0+rocm7.11.0a20260110` as `uv pip install` or `pip install` works (from `https://rocm.nightlies.amd.com/v2/gfx110X-all/`), but using this as a dependency in a `pyproject` doesn't work.

### Operating System

10.0.26100

### CPU

AMD Ryzen 7 PRO 8840U w/ Radeon 780M Graphics

### GPU

AMD Radeon 780M Graphics

### ROCm Version

nightly

### ROCm Component

_No response_

### Steps to Reproduce

<details>
<summary>Use this `pyproject.toml`</summary>


```toml
[project]
name = "test_torch"
version = "0.1.0"
description = "Test torch"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
  "rocm[libraries,devel]==7.11.0a20260110",
  "torch==2.11.0a0+rocm7.11.0a20260110",
]
classifiers = ["Private :: Do Not Upload"]

[build-system]
requires = [
  "cmake",
  "ninja",
  "numpy",
  "rocm[libraries,devel]==7.11.0a20260110 ; platform_system == 'Windows'",
  "setuptools-scm",
  "setuptools",
  "torch==2.11.0a0+rocm7.11.0a20260110 ; platform_system == 'Windows'",
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["test_torch"]

[tool.setuptools.package-dir]
test_torch = "src/test_torch"

[tool.uv]
prerelease = "allow"

[tool.uv.sources]
torch = [
  { index = "rocm-nightly-gfx110X" },
]
rocm = [
  { index = "rocm-nightly-gfx110X" },
]

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
explicit = true

[[tool.uv.index]]
name = "rocm-nightly-gfx110X"
url = "https://rocm.nightlies.amd.com/v2/gfx110X-all/"
# explicit = true
```

</details>

and simply run `uv sync`. Error would be similar to the following:
```
Using CPython 3.12.10 interpreter at: C:\Python312\python.exe
Creating virtual environment at: .venv
Resolved 16 packages in 2.98s
error: Distribution `triton==3.6.0+gite1aca0a7.rocm7.11.0a20260110 @ registry+https://rocm.nightlies.amd.com/v2/gfx110X-all/` can't be installed because it doesn't have a source distribution or wheel for the current platform

hint: You're on Windows (`win_amd64`), but `triton` (v3.6.0+gite1aca0a7.rocm7.11.0a20260110) only has wheels for the following platform: `linux_x86_64`; consider adding your platform to `tool.uv.required-environments` to ensure uv resolves to a version with compatible wheels
```

### (Optional for Linux users) Output of /opt/rocm/bin/rocminfo --support

_No response_

### Additional Information

_No response_