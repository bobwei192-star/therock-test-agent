https://github.com/langchain-ai/deepagents.git 如何二次开发
# Test Case Agent 需求文档


## 商业价值
1. 降本：降低机器成本与人力成本
手写一个ROCm用例需数小时，Agent一句话自动闭环。效率提升10倍以上，工程师转为最终审核者，专家时间释放至高ROI工作，高质量的
测试用例，有效减少机器测试时间。

2. 提质：用例从经验驱动到资产驱动
RAG知识库保障用例贴合官方规范、风格统一、语法可校验。一次性通过率提升，团队知识沉淀为可执行资产，消除人员离职造成的知识断层。

3. 提效：验证周期从天到分钟
Agent自动接管"生成→执行→失败修复"全流程。端到端验证从以天计压缩至分钟级，加速模型与算子交付。

4. 增值：质量保障从人力线性到算力弹性
Agent可并行批量生成用例，测试覆盖率随版本快速规模化。降低因覆盖不足导致的线上故障风险与品牌损失。

核心指标：ROI提升、迭代加速、故障风险降低。




## 项目定位

基于 **LangGraph + LangChain** 构建的智能体，自动生成、执行、修复 AMD ROCm 及 AI Model（运行于 ROCm）的测试用例。

**脚手架**：langgraph-kit（Agent 全栈工具包）

---

## 核心功能

### 1. 生成测试用例

- 用户以自然语言描述测试需求，Agent 生成 **pytest 格式可执行代码** + 简要说明
- 覆盖范围：
  - ROCm 底层算子：rocBLAS、MIOpen、rocSOLVER 等
  - AI Model 推理：vLLM / PyTorch 推理服务

### 2. （结果驱动）修复用例

- AIDevOps 返回测试失败 → Agent 自动分析失败原因 → 修复用例

### 3. 生成并执行

- 在功能 1 基础上，自动调用 **AIDevOps Agent（已有外部系统）** 执行生成的测试用例，返回执行结果

### 4. （用户驱动）修复用例

- 用户描述用例问题 → Agent 分析 → 修复



---

## 交互界面

**P0 ~ P2**：CLI 命令行（终端对话式）

**P3**：扩展 Web 界面（FastAPI + 前端）

---

## 知识库

Agent 需挂载 **RAG 知识库**，确保生成的用例贴合官方规范：

| 知识来源 | 内容 |
|---------|------|
| ROCm 官方文档 | API 签名、参数约束、数据类型、性能基线 |
| vLLM 文档 | 启动参数、模型支持列表、benchmark 方式 |
| 现存用例仓库 | 社区已有 pytest 用例，作为风格和结构参考 |

知识库更新方式：定期拉取上游仓库 → 向量化入库 → Agent 生成时主动检索。

---

## 工具集

Agent 可调用的工具：

| 工具 | 用途 |
|------|------|
| RAG 检索 | 从知识库获取 ROCm 规范 |
| 文件读写 | 保存/更新生成的 `.py` 用例文件 |
| A2A 任务委托 | 向 AIDevOps 提交执行请求 |
| A2A 结果接收 | 获取 AIDevOps 回传的执行结果 |
工具/技术	用途	备注
LangGraph	Agent 编排	脚手架：langgraph-kit
RAG (ChromaDB)	知识检索	挂载 ROCm 文档/社区用例
A2A Protocol	与 AIDevOps 通信	任务委托 + 结果回传
AST Parser	代码校验	确保生成的 pytest 无语法错误

---

## 核心流程

```
用户需求
   │
   ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│  规划器   │ → │  生成器   │ → │  验证器   │
│ 拆解任务  │   │ 生成代码  │   │ 校验格式  │
└──────────┘    └──────────┘    └──────────┘
       ↑              │
       │    ┌─────────┘
       │    ▼
       │  ┌──────────┐    ┌──────────────┐
       │  │ 执行用例  │ → │ AIDevOps Agent │ (已有外部系统)
       │  └──────────┘    └──────────────┘
       │        │
       │        ▼ (失败)         ▼ (通过)
       │  ┌──────────┐      ┌──────────┐
       └──│ 自动修复  │      │  完成 ✅  │
          └──────────┘      └──────────┘
               │
               └──→ 修复后自动重新提交执行（最多 3 轮）
```

**修复循环规则**：
- AIDevOps 返回失败 → Agent 分析失败日志 → 修复用例 → 自动重新提交
- 最多重试 **3 轮**，超过后交由人工处理
- 通过后结束流程，用例文件落盘保存

---

## 技术栈

| 层级 | 选型 |
|------|------|
| Agent 编排 | LangGraph |
| LLM 调用 | LangChain + OpenAI 兼容接口 |
| 项目脚手架 | langgraph-kit |
| 用例格式 | pytest（`.py`） |
| Agent 间通信 | **A2A (Agent-to-Agent Protocol)** |
| 外部依赖 | AIDevOps Agent（已有，通过 A2A 协议对接） |

---

## Agent 间通信

本项目 Agent 与 AIDevOps Agent 之间采用 **A2A 协议**：

- **Test Case Agent** 通过 A2A 向 AIDevOps 发起任务委托（提交 pytest 用例 → 请求环境执行）
- **AIDevOps** 执行完成后通过 A2A 回传结果（pass/fail + 日志 + 性能数据）
- A2A 提供能力发现、任务委托、流式反馈能力，无需底层协议适配

---

## 用例示例

**输入**：
> 为 vLLM 上运行的 Llama-3.1-8B 生成推理性能测试

**输出**：pytest 文件，例如：

```python
# test_vllm_llama31_8b_performance.py
# 测试目标：Llama-3.1-8B 在 vLLM 上的推理吞吐量
# 预期：TTFT < 200ms, TPOT < 50ms

import pytest
import requests

VLLM_ENDPOINT = "http://localhost:8000/v1/completions"

@pytest.mark.parametrize("num_prompts", [1, 10, 100])
def test_inference_throughput(num_prompts):
    ...
```

---

## 里程碑

| 阶段 | 目标 | 核心产出 |
|------|------|---------|
| **P0** | 基础生成 | 知识库搭建 → CLI 对话 → 自然语言生成 pytest 用例 → 文件落盘 |
| **P1** | 对接执行 | A2A 集成 → 生成用例后自动提交 AIDevOps 执行 → 接收并展示结果 |
| **P2** | 自动修复 | 失败分析 → 用例修复 → 自动重提交（最多 3 轮重试循环） |
| **P3** | 闭环迭代 | 用例仓库管理 → Web 界面 → CI 自动触发 → 失败自动修复 → 人工兜底 |

---

## 非功能需求

| 维度 | 要求 |
|------|------|
| 可靠性 | 生成用例需通过语法校验（AST 解析），不可包含不可执行代码 |
| 容错 | AIDevOps 不可达时，降级为仅生成本地用例，不阻塞流程 |
| 可追溯 | 每次生成/执行/修复记录保存为文件或 Memory，支持回查 |




第一个大坑是token消耗。AI Agent在处理复杂任务时，会消耗大量的token，成本很快就上去了。我的解决方案是：

1. 优化prompt，减少不必要的输出
2. 使用更便宜的模型处理简单任务
3. 实现token使用量监控和限制
第二个问题是模型幻觉。有时候Agent会给出看起来很合理但实际上错误的信息，这在运维场景下是很危险的。我的应对策略是：


3. 建立知识库，让Agent基于已知信息回答问题

第三个问题是稳定性。API服务偶尔会出现故障或限流，影响Agent的正常工作。我的解决方案包括：

1. 配置多个API服务作为备份
2. 实现请求重试机制
3. 添加降级处理逻辑


https://github.com/datawhalechina/hello-agents

https://hello-agents.datawhale.cc/#/README.md
