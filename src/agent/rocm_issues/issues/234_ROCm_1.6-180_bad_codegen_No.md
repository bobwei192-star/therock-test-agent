# ROCm 1.6-180 bad codegen? No

> **Issue #234**
> **状态**: closed
> **创建时间**: 2017-10-24T10:59:16Z
> **更新时间**: 2018-06-03T15:18:30Z
> **关闭时间**: 2018-06-03T15:18:30Z
> **作者**: preda
> **标签**: Bug_Functional_Issue
> **URL**: https://github.com/ROCm/ROCm/issues/234

## 标签

- **Bug_Functional_Issue** (颜色: #d93f0b)

## 描述

On Ubuntu 16.04.3, R9-Nano, ROCm 1.6-180,
It seems to me there's a case of bad code generation.

The code is trimmed down from an NTT FFT that I develop (an FFT with modular arithmetic), I prepared a small test case here (with build instructions included):
https://github.com/preda/gpuowl/tree/master/bug

Let me explain why I think there's a problem:

in the kernel, commenting or un-commenting the barrier() right after the shufl() changes the result. IMO, that barrier is not needed and should not affect the result. The different outputs with/without the barrier indicates that at least one of the outputs is wrong.
```
kernel __attribute__((reqd_work_group_size(256, 1, 1))) void bug(global T2 *io, global T2 *trig) {
  local uint lds[8 * 256];
  uint me = get_local_id(0);
  T2 u[8];
  for (int i = 0; i < 8; ++i) { u[i] = io[256 * i + me]; }
  fft8(u);
  shufl(lds, u, 8, 32);

  // Comment or un-comment the next barrier() to observe different behavior.
  // barrier(CLK_LOCAL_MEM_FENCE);

  tabMul(trig, u, 8, 32);
  for (int i = 0; i < 8; ++i) { io[256 * i + me] = u[i]; }  
}
```

Now, for why the barrier() after shufl() isn't needed: after the last write to LDS there is a barrier(). So no thread changes LDS anymore after that.

After the final reading from LDS, each thread uses only what it itself read, thus no need for another barrier() after LDS read.

Please consider this, to me it clearly looks like it's P0 material.


---

## 评论 (23 条)

### 评论 #1 — gstoner (2017-10-24T17:41:59Z)

The team is looking at it now.   

---

### 评论 #2 — gstoner (2017-10-24T19:55:11Z)

From Compiler Engineering - With wg size of 256, second loop for some subgroups can start execution before the first finished in all of them. I.e. barrier is necessary.

---

### 评论 #3 — preda (2017-10-24T21:16:39Z)

Thanks for looking into this so promptly! But why is the barrier necessary?
This is a schematic:
1. write to LDS
2. barrier()
3. read from LDS
4. [barrier()?]
5. use what was read from LDS
Clearly the barrier at step 2 is needed. But is the one at step 4 needed?

Let's assume there is no barrier at step 4, then some subgroups start 5 before all finished 3. But that is correct! because step 5 is not affected by other subgroups having finished or not step 3.

Thank you. I would love to be wrong on this issue, and the problem be only my misunderstanding.


---

### 评论 #4 — preda (2017-10-24T21:30:16Z)

```
  for (uint i = 0; i < n; ++i) { lds[(m + i * 256 / f) / n * f + m % n * 256 + me % f] = ((T *) (u + i))[0]; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { ((T *) (u + i))[0] = lds[i * 256 + me]; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { lds[(m + i * 256 / f) / n * f + m % n * 256 + me % f] = ((T *) (u + i))[1]; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { ((T *) (u + i))[1] = lds[i * 256 + me]; }

  // barrier(CLK_LOCAL_MEM_FENCE); // Comment or un-comment this barrier() to observe different behavior.

  tabMul(trig, u, 8, 32);
  for (int i = 0; i < 8; ++i) { io[256 * i + me] = u[i]; }
```
I inlined the shufl() loop for clarity.

The only barrier() that I question is the very last one, which is commented out. Do you say that one is needed -- why?


---

### 评论 #5 — boxerab (2017-10-24T21:32:54Z)

I think you have a bug in the code.

Should it not be:

     for (int i = 0; i < 8; ++i) { io[i + me * 8] = u[i]; }  

?

`256 * i + me` can be out of bounds for the given array size, for example when `i =7` and `me = 256`




---

### 评论 #6 — boxerab (2017-10-24T21:34:58Z)

Oh, never mind :)


---

### 评论 #7 — preda (2017-10-25T02:08:44Z)

I need to update my observations: it seems that adding the barrier() after the final LDS read fixes my code.
But why would that be needed?

I'll keep investigating. If the barrier fixes it, at least I have a workaround. I think you still have the burden to make sure that barrier is actually required for a valid reason, otherwise it's just a workaround for a bug.

---

### 评论 #8 — preda (2017-10-26T08:15:57Z)

The barrier() in question isn't actually needed. (and I'd like to hear the argument to the contrary).

The "bug fixing" behavior of the barrier() was due to it also implying a mem_fence(). The weaker mem_fence is enough to workaround as well.

The updated excerpt is:
```
  for (uint i = 0; i < n; ++i) { lds[(m + i * 256 / f) / n * f + m % n * 256 + me % f] = u[i].x; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { u[i].x = lds[i * 256 + me]; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { lds[(m + i * 256 / f) / n * f + m % n * 256 + me % f] = u[i].y; }
  barrier(CLK_LOCAL_MEM_FENCE);
  for (uint i = 0; i < n; ++i) { u[i].y = lds[i * 256 + me]; }
  
  // mem_fence(CLK_LOCAL_MEM_FENCE); // Comment or un-comment this to observe different behavior.

  tabMul(trig, u, 8, 32);
  for (int i = 0; i < 8; ++i) { io[256 * i + me] = u[i]; }
```

Of course, the question is *why* not putting a mem_fence() in that particular spot produces bad behavior. To me, it still looks like it's on the compiler, but I'm open to an explanation.

In the meantime, I'd propose re-qualifying this bug as valid.


---

### 评论 #9 — preda (2017-10-26T23:39:35Z)

Ping? please either a confirmation of "invalid" or re-evaluation.


---

### 评论 #10 — gstoner (2017-10-27T00:21:48Z)


a global barrier is needed, but the barrier is both a fence and execution barrier. The array io can be accessed out of order.

For the kernel above,  what is needed is barrier aspect of it. Memfence technically also needed, but global. Although one can be lucky with fence, it all depends on the behavior in with cache.

I am going to be offline a little bit I am bit under the weather. 

---

### 评论 #11 — preda (2017-10-27T00:53:42Z)

Yes I understand if you're away, please take care and get better.

Clearly I'm not proposing to remove *all* the barriers, only the very last one is in question.

Because of the first barrier (which stays) all the subgroups are done reading from the array io, and done writing what they read to LDS, before any subgroup starts writing to the array io.

So, how does your explanation work, what do you mean "the array io can be access out of order"? because all the subgroups are done reading, before any start writing to that array.


---

### 评论 #12 — preda (2017-10-27T01:00:53Z)

I updated the sample at https://github.com/preda/gpuowl/tree/master/bug
to use distinct global arrays for input and output. The bug remains.


---

### 评论 #13 — gstoner (2017-10-27T01:07:27Z)

did you look at this too.  http://rocm-documentation.readthedocs.io/en/latest/GCN_ISA_Manuals/testdocbook.html#testdocbook

---

### 评论 #14 — gstoner (2017-10-27T01:13:54Z)

I sending to a different part of my compiler team tonight.   I am now really out 

---

### 评论 #15 — preda (2017-10-27T01:13:56Z)

Yes, many times. I've been reading the ISA docs since GCN 1.2 (Fiji), and been waiting with anticipation before the Vega ISA doc was released. (BTW, I've also found many typos and formatting errors in those docs, some persisting).

But, even if I had complete ignorance of the ISA, that would not by itself make this bug invalid.


---

### 评论 #16 — preda (2017-10-27T01:14:47Z)

Thanks! your help is appreciated.

---

### 评论 #17 — gstoner (2017-10-27T01:17:54Z)

I do not write the ISA doc,  Vega ISA  web doc if you see the issue it easy to fixes now.   The older documents were written in FrameMaker,  I think they lost skill to use the tools.  

No problem  I see what is in my box when I wake up.

---

### 评论 #18 — boxerab (2017-10-27T11:30:34Z)

@preda have you tried this with work group size = 64 ? In this case, barriers would be removed by the compiler, as wg size is <= wave front size, so do you get the correct result in this case ?

---

### 评论 #19 — preda (2017-10-27T23:14:35Z)

Yes, I can repro with WG==64 as well. I updated the bug-case with WG==64, and added a Makefile:
https://github.com/preda/gpuowl/tree/master/bug


---

### 评论 #20 — boxerab (2017-10-27T23:40:43Z)

@preda  that is strange, because in this case, there should be no barriers.  If you look at disassembly, do you see any? 

---

### 评论 #21 — boxerab (2017-10-27T23:41:27Z)

I suspect the problem is somewhere else, then.

---

### 评论 #22 — boxerab (2017-10-27T23:46:03Z)

You should really put brackets around different arithmetic operations to make sure you have the right
order. For example `%` is weaker than `*`, so put brackets to make sure the index you calculate is correct
and not out of bounds. Can you add checks to make sure that the local memory array indexes are always less than 8*256 ?

---

### 评论 #23 — preda (2017-10-28T08:37:01Z)

With workgroup size == 64 there are no barriers generated in the ISA, as expected.

I didn't say that this is a "barrier bug", I said it's a codegen bug. OTOH somebody from AMD said that it's just programmer error consisting in bad use of barriers by the programmer. Now with WG=64 that explanation becomes moot.

About order of operations ("operator precedence"), the order is well defined according to the C language. In particular, % has the same precedence as * and /, not weaker.
http://en.cppreference.com/w/c/language/operator_precedence

---
